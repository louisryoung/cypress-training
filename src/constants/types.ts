export type Staff = {
  [key: string]: any;
};

export type Option = {
  label: string;
  value: string;
};

export type Column = {
  id: number;
  field: string;
  headerName: string;
  checked: boolean;
};
